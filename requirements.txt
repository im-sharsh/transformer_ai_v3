# Core (Phase 1)
streamlit>=1.40
pandas>=2.1
numpy>=1.26
pyarrow>=14
pyyaml>=6
psutil>=5.9
# Used for CPU/GPU detection now and for the built-in transformer from Phase 5.
# CPU-only machines can install the smaller CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
# Leakage detection (Phase 4): univariate predictive-power checks (ROC-AUC)
scikit-learn>=1.3
# Tests
pytest>=8
